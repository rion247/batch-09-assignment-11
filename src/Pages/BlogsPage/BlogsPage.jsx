import { Helmet } from "react-helmet";

const BlogsPage = () => {
    return (
        <div>

            <Helmet>
                <title>Root Jobs | Blogs Page</title>
            </Helmet>

            <h1>Blogs Page</h1>

        </div>
    );
};

export default BlogsPage;