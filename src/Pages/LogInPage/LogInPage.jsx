import { Helmet } from "react-helmet";

const LogInPage = () => {
    return (
        <div>

            <Helmet>
                <title>Root Jobs | LogIn Page</title>
            </Helmet>
            
            <h1>Log In Page</h1>
        </div>
    );
};

export default LogInPage;