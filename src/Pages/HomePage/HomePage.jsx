import { Helmet } from "react-helmet";

const HomePage = () => {
    return (
        <div>

            <Helmet>
                <title>Root Jobs | Home Page</title>
            </Helmet>

            <h1>Home Page</h1>

        </div>
    );
};

export default HomePage;